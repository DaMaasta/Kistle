# URL des Node.js-Backends (nginx proxyt /api → webapp:3000)
VITE_API_URL=/api

# Google OAuth Client-ID (aus Google Cloud Console — gleiche wie im Backend)
VITE_GOOGLE_CLIENT_ID=deine-client-id.apps.googleusercontent.com
